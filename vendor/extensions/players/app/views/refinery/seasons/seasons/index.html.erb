<% content_for :body_content_left do %>
  <ul id="seasons">
    <% @seasons.each do |season| %>
      <li>
        <%= link_to season, refinery.seasons_season_path(season) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
